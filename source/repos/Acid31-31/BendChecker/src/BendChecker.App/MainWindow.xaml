<Window x:Class="BendChecker.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="BendChecker (V1)" Height="520" Width="820">
  <Grid Margin="12">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
    </Grid.RowDefinitions>

    <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,8">
      <TextBlock Width="120" VerticalAlignment="Center" Text="STEP:"/>
      <TextBox x:Name="StepPathText" Width="540" Margin="0,0,8,0"/>
      <Button Content="Wählen…" Width="100" Click="PickStep_Click"/>
    </StackPanel>

    <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,8">
      <TextBlock Width="120" VerticalAlignment="Center" Text="Regeln (Excel):"/>
      <TextBox x:Name="RulesPathText" Width="540" Margin="0,0,8,0"/>
      <Button Content="Wählen…" Width="100" Click="PickRules_Click"/>
    </StackPanel>

    <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,0,0,8">
      <TextBlock Width="120" VerticalAlignment="Center" Text="Material:"/>
      <ComboBox x:Name="MaterialBox" Width="200" SelectedIndex="0">
        <ComboBoxItem Content="Stahl"/>
        <ComboBoxItem Content="ALU"/>
        <ComboBoxItem Content="V2a"/>
        <ComboBoxItem Content="Sen.Verzinkt"/>
      </ComboBox>

      <TextBlock Width="120" Margin="24,0,0,0" VerticalAlignment="Center" Text="Dicke (mm):"/>
      <TextBox x:Name="ThicknessText" Width="80" Text="1,00"/>

      <TextBlock Width="80" Margin="24,0,0,0" VerticalAlignment="Center" Text="Prisma V:"/>
      <TextBox x:Name="PrismaVText" Width="80" Text=""/>
    </StackPanel>

    <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,0,0,8">
      <Button Content="Analysieren" Width="140" Click="Analyze_Click"/>
      <TextBlock x:Name="StatusText" Margin="12,0,0,0" VerticalAlignment="Center"/>
    </StackPanel>

    <TextBlock Grid.Row="4" FontWeight="Bold" Text="Ergebnis / Findings:" Margin="0,8,0,6"/>

    <DataGrid Grid.Row="5" x:Name="FindingsGrid" AutoGenerateColumns="False" IsReadOnly="True">
      <DataGrid.Columns>
        <DataGridTextColumn Header="Severity" Binding="{Binding Severity}" Width="100"/>
        <DataGridTextColumn Header="Code" Binding="{Binding Code}" Width="160"/>
        <DataGridTextColumn Header="Message" Binding="{Binding Message}" Width="*"/>
      </DataGrid.Columns>
    </DataGrid>
  </Grid>
</Window>
