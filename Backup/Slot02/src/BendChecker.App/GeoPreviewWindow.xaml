<Window x:Class="BendChecker.App.GeoPreviewWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="GEO Vorschau" Height="720" Width="980"
        WindowStartupLocation="CenterOwner">
  <Grid Margin="10">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
    </Grid.RowDefinitions>

    <TextBlock x:Name="InfoText" FontWeight="SemiBold" Margin="0,0,0,8" Text="GEO Vorschau"/>

    <Grid Grid.Row="1">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="2*"/>
        <ColumnDefinition Width="10"/>
        <ColumnDefinition Width="*"/>
      </Grid.ColumnDefinitions>

      <Border Grid.Column="0" BorderBrush="#33000000" BorderThickness="1" Background="#FFF8F9FB">
        <Viewbox Stretch="Uniform" StretchDirection="Both">
          <Canvas x:Name="PreviewCanvas" Width="900" Height="600" Background="#FFF8F9FB"/>
        </Viewbox>
      </Border>

      <TextBox Grid.Column="2"
               x:Name="RawGeoText"
               IsReadOnly="True"
               FontFamily="Consolas"
               FontSize="12"
               TextWrapping="NoWrap"
               HorizontalScrollBarVisibility="Auto"
               VerticalScrollBarVisibility="Auto"/>
    </Grid>
  </Grid>
</Window>
