<Window x:Class="BendChecker.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="BendChecker (3D Preview)" Height="760" Width="1280"
        Loaded="Window_Loaded"
        d:DataContext="{d:DesignInstance Type=Window}">
  <Grid Margin="12" d:Background="#FFF7F7F7">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
    </Grid.RowDefinitions>

    <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,8">
      <TextBlock Width="120" VerticalAlignment="Center" Text="STEP:"/>
      <TextBox x:Name="StepPathText" Width="540" Margin="0,0,8,0" d:Text="E:\Konstruktion\Teil_001.step"/>
      <Button Content="Waehlen..." Width="100" Click="PickStep_Click"/>
    </StackPanel>

    <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,8">
      <TextBlock Width="120" VerticalAlignment="Center" Text="Regeln (Excel):"/>
      <Button Content="Datei waehlen..." Width="130" Click="PickRules_Click"/>
      <TextBox x:Name="RulesPathText" Visibility="Collapsed"/>
      <TextBlock x:Name="RulesInfoText" Margin="12,0,0,0" VerticalAlignment="Center" Text="Keine Regeln importiert."/>
    </StackPanel>

    <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,0,0,8">
      <TextBlock Width="120" VerticalAlignment="Center" Text="Material:"/>
      <ComboBox x:Name="MaterialBox" Width="200" SelectedIndex="0">
        <ComboBoxItem Content="Stahl"/>
        <ComboBoxItem Content="ALU"/>
        <ComboBoxItem Content="V2a"/>
        <ComboBoxItem Content="Sen.Verzinkt"/>
      </ComboBox>
      <TextBlock Width="120" Margin="24,0,0,0" VerticalAlignment="Center" Text="Dicke (mm):"/>
      <TextBox x:Name="ThicknessText" Width="80" Text="1,00"/>
      <TextBlock Width="80" Margin="24,0,0,0" VerticalAlignment="Center" Text="Prisma V:"/>
      <TextBox x:Name="PrismaVText" Width="80" Text="" d:Text="12"/>
    </StackPanel>

    <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,0,0,8">
      <Button Content="Analysieren" Width="140" Click="Analyze_Click"/>
      <Button Content="Zoom +" Width="80" Margin="12,0,0,0" Click="ZoomIn_Click"/>
      <Button Content="Zoom -" Width="80" Margin="6,0,0,0" Click="ZoomOut_Click"/>
      <Button Content="Ansicht Reset" Width="110" Margin="6,0,0,0" Click="ResetView_Click"/>
      <TextBlock Margin="12,0,0,0" VerticalAlignment="Center" Text="Maus: Links=drehen, Rad=zoomen"/>
      <TextBlock x:Name="StatusText" Margin="12,0,0,0" VerticalAlignment="Center" d:Text="Designer-Vorschau aktiv"/>
    </StackPanel>

    <TextBlock Grid.Row="4" FontWeight="Bold" Text="3D-Vorschau / Findings:" Margin="0,8,0,6"/>

    <Grid Grid.Row="5">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="2*"/>
        <ColumnDefinition Width="12"/>
        <ColumnDefinition Width="*"/>
      </Grid.ColumnDefinitions>

      <Border Grid.Column="0" BorderBrush="#33000000" BorderThickness="1" CornerRadius="4" Background="#FF1E1E1E" MinHeight="420">
        <Grid>
          <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="170"/>
          </Grid.RowDefinitions>

          <Grid x:Name="PreviewHost" Grid.Row="0">
            <Image x:Name="PreviewSnapshotImage" Stretch="Uniform" Visibility="Collapsed" IsHitTestVisible="False"/>
            <Border x:Name="DesignerPreviewPlaceholder" Margin="12" CornerRadius="4" Background="#FF2A2E38">
              <Grid>
                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                  <TextBlock Text="Designer-Vorschau" Foreground="White" FontSize="22" FontWeight="SemiBold" HorizontalAlignment="Center"/>
                  <TextBlock Text="3D-Laufzeitansicht wird nur beim Start der App geladen." Foreground="#FFD0D0D0" Margin="0,8,0,0" HorizontalAlignment="Center"/>
                  <Border Width="260" Height="150" Margin="0,18,0,0" BorderBrush="#66FFFFFF" BorderThickness="1" Background="#FF404654">
                    <Grid>
                      <Rectangle Width="120" Height="70" Fill="#FFB9BECD" Stroke="#FFEEEEEE" StrokeThickness="1" RenderTransformOrigin="0.5,0.5">
                        <Rectangle.RenderTransform>
                          <TransformGroup>
                            <SkewTransform AngleX="-18"/>
                            <RotateTransform Angle="-8"/>
                          </TransformGroup>
                        </Rectangle.RenderTransform>
                      </Rectangle>
                    </Grid>
                  </Border>
                </StackPanel>
              </Grid>
            </Border>
          </Grid>

          <Grid Grid.Row="1" Margin="10,8,10,10">
            <Grid.RowDefinitions>
              <RowDefinition Height="Auto"/>
              <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid Grid.Row="0" Margin="0,0,0,6">
              <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="8"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="8"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="8"/>
                <ColumnDefinition Width="Auto"/>
              </Grid.ColumnDefinitions>
              <TextBlock Text="Visualisierungs-Report" Foreground="#FFDDE3F0" FontWeight="SemiBold" VerticalAlignment="Center"/>
              <Button Grid.Column="1" Content="Alles markieren" Padding="10,2" Click="SelectAllReport_Click"/>
              <Button Grid.Column="3" Content="Report kopieren" Padding="10,2" Click="CopyReport_Click"/>
              <Button Grid.Column="5" Content="Report speichern" Padding="10,2" Click="SaveReport_Click"/>
              <Button Grid.Column="7" Content="Ordner öffnen" Padding="10,2" Click="OpenReportFolder_Click"/>
            </Grid>
            <TextBox x:Name="VisualizationReportText"
                     Grid.Row="1"
                     FontFamily="Consolas"
                     FontSize="12"
                     IsReadOnly="True"
                     IsReadOnlyCaretVisible="True"
                     TextWrapping="Wrap"
                     VerticalScrollBarVisibility="Auto"
                     HorizontalScrollBarVisibility="Auto"
                     Background="#FF101216"
                     Foreground="#FFDDE3F0"
                     BorderBrush="#88778AA8"
                     BorderThickness="1"/>
          </Grid>
        </Grid>
      </Border>

      <Grid Grid.Column="2">
        <Grid.RowDefinitions>
          <RowDefinition Height="*"/>
          <RowDefinition Height="8"/>
          <RowDefinition Height="220"/>
        </Grid.RowDefinitions>

        <DataGrid Grid.Row="0" x:Name="FindingsGrid" AutoGenerateColumns="False" IsReadOnly="True">
          <DataGrid.Columns>
            <DataGridTextColumn Header="Severity" Binding="{Binding Severity}" Width="100"/>
            <DataGridTextColumn Header="Code" Binding="{Binding Code}" Width="160"/>
            <DataGridTextColumn Header="Message" Binding="{Binding Message}" Width="*"/>
          </DataGrid.Columns>
        </DataGrid>

        <Border Grid.Row="2" BorderBrush="#33000000" BorderThickness="1" CornerRadius="4" Background="#FFF8F9FB" Padding="8">
          <Grid>
            <Grid.RowDefinitions>
              <RowDefinition Height="Auto"/>
              <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid>
              <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="8"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="8"/>
                <ColumnDefinition Width="Auto"/>
              </Grid.ColumnDefinitions>
              <TextBlock Text="Abwicklungsvorschau" FontWeight="SemiBold" VerticalAlignment="Center"/>
              <Button Grid.Column="1" Content="Geo laden..." Padding="10,2" Click="LoadGeoPreview_Click"/>
              <Button Grid.Column="3" Content="Als .geo speichern" Padding="10,2" Click="ExportFlatPatternGeo_Click"/>
              <Button Grid.Column="5" Content=".geo Vorschau" Padding="10,2" Click="OpenGeoPreview_Click"/>
            </Grid>
            <Grid Grid.Row="1" Margin="0,6,0,0">
              <Image x:Name="FlatPatternImage" Stretch="Uniform"/>
              <TextBlock x:Name="FlatPatternInfoText"
                         HorizontalAlignment="Left"
                         VerticalAlignment="Top"
                         Margin="8"
                         Background="#CCFFFFFF"
                         Padding="6,4"
                         Foreground="#66000000"
                         Text="Noch keine Abwicklung berechnet."/>
            </Grid>
          </Grid>
        </Border>
      </Grid>
    </Grid>
  </Grid>
</Window>
